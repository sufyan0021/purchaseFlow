import React from 'react';
import Button from '@mui/joy/Button';


function PrimaryButton({text}) {
  return (
    <Button onClick={function(){}} size="md">
        {text}
    </Button>
  )
}

export default PrimaryButton